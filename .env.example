# Production Supabase project values. The anon key is safe for a browser when RLS is enabled.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Development only. Vite deliberately refuses demo mode in a production build.
VITE_DEMO_MODE=false
